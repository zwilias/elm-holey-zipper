{
    "dependencies": {
        "elm": "^0.19.0-bugfix6",
        "elm-format": "^0.8.1",
        "elm-test": "^0.19.0-rev4",
        "elm-verify-examples": "^3.0.1"
    },
    "scripts": {
        "tests": "elm-test",
        "check-format": "elm-format --validate src",
        "examples": "elm-verify-examples",
        "test": "npm run check-format && npm run examples && npm run tests"
    }
}
